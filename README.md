Create a readme file and answer this question on your own. Don’t copy-paste from Google or any AI chatbot.



1️⃣ What is the difference between var, let, and const?
## 1. Answer: var is older variable & this time it's use for your fault because it's older version modern version is let. let is changeable variable you can change your variable property when you use this let property. const is not changeable variable property there is put lot of value but not change it in this property we are using this variable many time. let and const is the best for every programmers.

2️⃣ What is the spread operator (...)?
## 2. Answer: the spread operator is used to javascript to use for expand array, object or irretable or individual elements. you have an array your can put this array name like: let arr = [2, 3, 4, 5, , 7]: let arr2 = [2, 3, 5,6,2]; console.log("...arr, ...arr2") // output is: [2, 3, 4, 5, , 7, 2, 3, 5, 6, 2]; ......  this type you can expand this operator.

3️⃣ What is the difference between map(), filter(), and forEach()?
## 3. Answer: map(), filter() and forEach()  method is loop through arrays but they behave differently. map() method is used for transform every were. it's returns a new array. the array length stays the same. filter() method is used when you want to keep only certain elements based on a condition. it's returns a new array. the array like smaller.

4️⃣ What is an arrow function?
## 4. Answer: arrow function is shorter to cleaner way to write javascript function this method look like this type: const a () => {}; it's very cleaner function in javascript.

5️⃣ What are template literals?
## 5. Answer: Template Literals are a way to write strings in JavaScript more easily and dynamically. They were introduced in ES6 and use backticks (` `) instead of quotes ("" or ''). They allow you to: Embed variables inside strings Write multi-line strings Use expressions inside strings


